import React from 'react';
import Layout from "../components/Layout";

type Props = {};

export default function AstronautsPage({ }: Props) {
    return (
        <Layout>AstronautsPage</Layout>
    );
}